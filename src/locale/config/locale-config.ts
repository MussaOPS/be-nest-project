//todo: реализовать
/*
import * as path from "path";
import {Module} from "@nestjs/common";

@Module({
    imports: [
        I18nModule.forRoot({
            fallbackLanguage: 'en', // Язык по умолчанию
            parser: I18nJsonParser,
            parserOptions: {
                path: path.join(__dirname, '../i18n/'), // Путь к директории с файлами локализации
            },
        }),
    ],
})

export class LocaleConfig {}
*/
