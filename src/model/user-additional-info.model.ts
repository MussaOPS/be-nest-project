export class UserAdditionalInfoModel {
    avatar: string;
    address: string;

    constructor(avatar: string, address: string) {
        this.avatar = avatar;
        this.address = address;
    }
}
